{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Cors": {
    // Vite dev server and `npm run preview`.
    "AllowedOrigins": [
      "http://localhost:5173",
      "http://localhost:8080"
    ]
  },
  "Proxy": {
    "HttpPort": 3128,
    "HttpsPort": 3127,
    // `npm run preview` in ../frontend binds 8080 on a developer machine, so the API
    // takes a port of its own here.
    "ApiPort": 5284
  },
  "Jwt": {
    "Key": "111niS2tzs1fdayaokMb2ErqZEudnhOOBQRZCx2LZis=", // Don't try. This is overwritten in prod ;-)
    "Issuer": "Backend",
    "Audience": "BackendApp"
  }
}
